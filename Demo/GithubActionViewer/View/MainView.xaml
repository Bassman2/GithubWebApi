<AppWindowView x:Class="GithubActionViewer.View.MainView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:b="http://schemas.microsoft.com/xaml/behaviors"
        xmlns:local="clr-namespace:GithubActionViewer.View"
        xmlns:vm="clr-namespace:GithubActionViewer.ViewModel" 
        d:DataContext="{d:DesignInstance Type=vm:MainViewModel}"
        mc:Ignorable="d"
        Height="450" Width="800">
    <Window.Resources>
        <Image x:Key="closeImage" Width="16" Height="16" Source="/Images/Close16.png" x:Shared="false"/>
        <Image x:Key="scanImage" Width="16" Height="16" Source="/Images/Scan16.png" x:Shared="false"/>
        <Image x:Key="helpImage" Width="16" Height="16" Source="/Images/Help16.png" x:Shared="false"/>
        <Image x:Key="infoImage" Width="16" Height="16" Source="/Images/Info16.png" x:Shared="false"/>
        <BoolToVisibilityCollapsedConverter x:Key="BoolToVisibilityCollabsedConverter"/>
    </Window.Resources>
    <DockPanel>
        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_File">
                <!--<MenuItem Header="Edit" Icon="{StaticResource editImage}" Command="{Binding EditCommand}" />-->
                <Separator/>
                <MenuItem Header="_Exit" Icon="{StaticResource closeImage}" Command="{Binding ExitCommand}" />
            </MenuItem>
        </Menu>
        <ToolBarTray DockPanel.Dock="Top">
            <ToolBar>
                <Button Command="{Binding UpdateCommand}" Content="{StaticResource scanImage}" ToolTip="Update Runs"/>                
            </ToolBar>
            <!--
            <ToolBar ItemsSource="{Binding Documentations}">
                <ToolBar.ItemTemplate>
                    <DataTemplate>
                        <Button Content="{Binding Icon}" ToolTip="{Binding Name}" Command="{Binding OpenCommand}" BorderBrush="{x:Null}" Background="{x:Null}" BorderThickness="0" Width="20" Height="20" />
                    </DataTemplate>
                </ToolBar.ItemTemplate>
            </ToolBar>-->
            <ToolBar>
                <Button Command="{Binding AboutCommand}" Content="{StaticResource infoImage}" ToolTip="About"/>
            </ToolBar>
        </ToolBarTray>
        <StatusBar DockPanel.Dock="Bottom">
            <StatusBar.ItemsPanel>
                <ItemsPanelTemplate>
                    <Grid>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="*" />
                            <ColumnDefinition Width="Auto" />
                            <ColumnDefinition Width="10" />
                        </Grid.ColumnDefinitions>
                    </Grid>
                </ItemsPanelTemplate>
            </StatusBar.ItemsPanel>
            <StatusBarItem Grid.Column="0">
                <TextBlock Text="{Binding StatusText}"/>
            </StatusBarItem>
            <StatusBarItem Grid.Column="1">
                <!--  -->
                <ProgressBar Grid.Row="0" Grid.Column="1" Visibility="{Binding IsScanning, Converter={StaticResource BoolToVisibilityCollabsedConverter}}" IsIndeterminate="true" Width="200" Height="16" Margin="3" />
            </StatusBarItem>
        </StatusBar>
        <DataGrid 
            ItemsSource="{Binding Actions}" 
            AutoGenerateColumns="False"
            HeadersVisibility="Column"
            GridLinesVisibility="None"
            CanUserAddRows="False"
            CanUserDeleteRows="False"
            CanUserResizeColumns="True"
            CanUserReorderColumns="True"
            CanUserSortColumns="True"
            IsSynchronizedWithCurrentItem="True"
            ClipboardCopyMode="IncludeHeader"
            Margin="10">
            <b:Interaction.Behaviors>
                <DataGridColumnVisibilityBehavior SettingsName="MembersColumnVisibility"/>
            </b:Interaction.Behaviors>
            <DataGrid.Columns>
                <DataGridTextColumn Header="Server" Binding="{Binding ServerName}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Owner" Binding="{Binding Owner}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Repository" Binding="{Binding RepositoryName}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Workflow" Binding="{Binding WorkflowName}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Run" Binding="{Binding Run}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Branch" Binding="{Binding Branch}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Started" Binding="{Binding Started, StringFormat=yyyy-MM-dd}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Run Number" Binding="{Binding RunNumber}" IsReadOnly="True"/>
                <DataGridTextColumn Header="State" Binding="{Binding State}" IsReadOnly="True"/>
                <DataGridTextColumn Header="Conclusion" Binding="{Binding Conclusion}" IsReadOnly="True"/>
            </DataGrid.Columns>
        </DataGrid>
    </DockPanel>
</AppWindowView>
